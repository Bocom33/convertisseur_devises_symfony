{% extends 'base.html.twig' %}

{% block title %}Conversion devises{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Conversion devises ✅</h1>
Calcul de taux de change :

{{ form_start(form) }}
    
    <div>{{ form_row(form.valeur1) }} {{ form_row(form.devise1) }}</div> 

    + 

    <div>{{ form_row(form.valeur2) }} {{ form_row(form.devise2) }}</div>
    
    {{ form_row(form.calcul) }}
    
{{ form_end(form) }}

    {% if result is not null %}
        <p>Résultat: {{ result }} {{ devise }} </p>
    {% endif %}

</div>
{% endblock %}
